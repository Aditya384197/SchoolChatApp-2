// A sign-up made with this exact email is silently granted admin access
// (subject to the same one-time config/adminUid claim used everywhere else
// -- see lib/auth.js). No label or hint about this appears anywhere in the
// UI, as asked. Change this one line to the real email before building.
export const ADMIN_ACCESS_EMAIL = 'py409537@gmail.com';
